%tr
  %td
    -#=link_to orders_item.book do
    -#  =image_tag orders_item.product.image, width: '100px'
  %td
    =link_to [main_app, orders_item.product] do
      %h3= orders_item.product.title
    .description= orders_item.product.try(:short_description)
  %td $#{orders_item.cost}
  %td= orders_item.count
  %td= orders_item.total